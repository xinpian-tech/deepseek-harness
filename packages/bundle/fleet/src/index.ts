/**
 * @dsh-fleet/bundle — the fleet composition as a profile bundle. The package's
 * substance is its two patch documents (`no-messaging.cordis.patch.yml` and
 * `cordis.patch.yml`), declared through the `dsh.bundle.patch` manifest field
 * and resolved by the profile composer through that field; this module carries
 * no runtime API.
 * @module @dsh-fleet/bundle
 */

export {}
